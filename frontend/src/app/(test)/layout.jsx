export const metadata = {
  title: 'API Tester | IS207',
  description: 'Internal API testing tool — Postman-like in browser',
};

export default function TestLayout({ children }) {
  return <>{children}</>;
}
